_chrome_tmp*/
_debug.txt
*.log
.DS_Store
Thumbs.db

# Raw GGG presskit (10-15 MB each, ~535 MB total). Only the resized
# images/presskit-web/ copies are checked in and served to GitHub Pages.
images/presskit/

# Sole build input for _tools/build-tree.js (source of truth; not loaded at
# runtime — the generated tree-data.js / tree-art.js / images/tree-ui/ ARE
# checked in). NOTE: map node→group via group.nodes, not the node.group field.
_pob2-tree.json
# Reference data, no longer consumed by the build:
#   tree-source.json   — RePoE-fork (original geometry lineage of tree-data.json)
#   tree-official.json — PoE1 tree
#   _pttree.json       — PoE1 tree GitHub listing
tree-source.json
tree-official.json
_pttree.json
_icon-list.txt
_stat_passive.json
_stat_general.json
_tools/node_modules/
_tools/dds/
_tools/_*.png
_tools/pob_*.lua
_tools/package-lock.json
